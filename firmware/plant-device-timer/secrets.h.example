#pragma once

const char* ssid     = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";

// Railway / Flask base URL (no trailing slash)
const char* API_BASE = "https://your-app.up.railway.app";

// After first successful pairing, token is stored in flash (Preferences).
// Optionally set a token here to skip NVS (same value as shown after dashboard Unpair is wrong — get from pairing response).
const char* DEVICE_TOKEN_OVERRIDE = "";

// Optional: paste 6-digit code here, upload once (token saved in flash).
const char* PAIRING_CODE_ON_BOOT = "";

// Default: captive portal — join Wi-Fi "PlantTimer-XXXX" on your phone,
// enter the dashboard pairing code at http://192.168.4.1
// Or Serial Monitor (115200): pair 123456
